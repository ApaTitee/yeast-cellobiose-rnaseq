# P2 出口验收记录（主分析）

**日期**：2026-09-14
**结论**：**PASS**

| # | 出口条件（PLAN §9） | 结果 | 凭证 |
| --- | --- | --- | --- |
| 1 | 参数敏感性 | **PASS** | `results/comparison/param_sensitivity.tsv`（k31 vs k25）、decoy 对照（TPM 中位 log2 差异 0.027） |
| 2 | 样本水平分析 | **PASS** | PC1 93.4%；组内 ρ 0.994–0.998 vs 组间 0.886–0.895；`results/figures/{pca,sample_correlation,clustering}` |
| 3 | 差异表达（主 + 敏感性 + 阈值对齐） | **PASS** | 主 644 DEG；配对 643（Jaccard 0.986，方向一致率 100%，LFC 相关 1.0000）；阈值对齐 632 |
| 4 | 模型诊断 | **PASS** | 无系统性反保守；Cook's cutoff 29.46，仅 1 个基因超阈；`results/figures/model_diagnostics/` |
| 5 | 功能分析 | **PASS** | GO slim / 全 GO（含层级传播）/ KEGG（已缓存）/ GSEA；`results/enrichment/` |
| 6 | LFC 口径写入决策记录 | **PASS** | 阈值一律用 MLE LFC；`scripts/04_differential_expression/14_deseq2_de.R` 内注释 + `docs/decisions/` |
| 7 | 背景集（universe）定义可见 | **PASS** | count ≥ 10 且 ≥ 3 样本 → 6049 基因；写入脚本与报告 |

## 关键结果

| 项 | 数值 |
| --- | --- |
| DEG（主 / 放宽 / 收紧 / 阈值对齐 / 配对） | 644 / 4583 / 363 / 632 / 643 |
| per-gene log2FC 与原文一致性 | Spearman **0.973**、Pearson 0.840（5771 基因） |
| DEG 重叠（632 vs 519） | **496**（原文 DEG 的 95.6%）、Jaccard 0.757、超几何 p ≈ 0 |
| 重叠内方向一致率 | **100%** |
| 富集层 | 复现原文两大结论（呼吸链/OXPHOS/ATP 上调；氨基酸与硫胺素合成下调） |

## 过程中发现并修正的问题

| 问题 | 处置 |
| --- | --- |
| SGD GAF 只含最具体术语注释、无祖先传播（GO:0008652 仅 3 行） | 新增 `16a_build_term2gene.R`：true-path-rule 传播（3 → 122 基因）；富集与记分卡共用该映射表 |
| `sub()` 被 clusterProfiler 依赖覆盖为 S4 泛型 + 变量名 `args` 与 base R 冲突 | 脚本改用 `base::sub` 与 `.cmdargs`，并在注释中说明 |
| 子文档中 `kable()` 探测不到 pandoc 目标格式，退化为 markdown 管道表 | `_setup.qmd` 改为 `knitr::asis_output()` 自建 HTML/Typst 表格输出 |
| `kable(format = "typst")` 在当前 knitr 版本不存在 | 同上（不再使用 kable） |
